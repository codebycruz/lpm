---
import { getCollection } from "astro:content";

const entries = await getCollection("learn");
const first = entries.sort((a, b) => a.data.order - b.data.order)[0];

return Astro.redirect(`/learn/${first.id}`);
---
